#include <iostream> 

int
main()
{
    std::cout << "Hello, World!\n";    // operator << ("put to") writes it second argument onto its first 
                                       // std::cout standard ouput stream
                                       // std:: specifies that the name cout is to be found in the standard-library namespace($3.4)
}